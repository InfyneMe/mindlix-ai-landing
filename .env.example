# Required. Keep this server-side and never expose it with a NEXT_PUBLIC_ prefix.
OPENROUTER_API_KEY=

# Optional. The default matches the configured OpenRouter integration.
# OPENROUTER_MODEL=openai/gpt-4o
